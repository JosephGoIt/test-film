// pagination-fetch.js
const paginationContainer = document.querySelector('.pagination-fetch_container');
window.globalCurrentPage = null;

export function paginationFetch(page, totalPages) {
  // Pagination logic
}

function onPagination({ target }) {
  // Pagination click event handler logic
}